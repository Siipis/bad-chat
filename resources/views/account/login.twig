layout: default
title: "{{ appName }}: Account Login"

===

<div class="container">
    <div class="col-sm-6 col-sm-offset-3">
        <h3>Account Panel</h3>

        {% partial 'alert' %}

        {% partial 'form/login' with {'url': 'account'} %}

        <hr/>

        <p class="text-center">
            <a href="{{ url('/') }}" class="btn btn-link btn-lg">
                <i class="glyphicon glyphicon-chevron-left"></i>
                Chat Login
            </a>
        </p>
    </div>
</div>